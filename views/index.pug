extends layout


block content
  include partials/header  
  h1 #{title}
  ul.list-group
    each product, i in products
      li.list-group-item
        a(href="/products/"+product._id)= product.title

